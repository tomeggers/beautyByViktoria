import React from 'react';
import Header from '../components/Header/Header';

function Book_now() {
    return (
        <div>
            <Header />
            <main>
                <h1>Book Now</h1>
                {/* Add your booking content here */}
            </main>
        </div>
    );
}
export default Book_now; // Corrected export name to match the file name